// Generated by CoffeeScript 1.6.2
(function() {
  var PAGE, pjax, showPage;

  PAGE = 'section';

  pjax = false;

  $(function() {
    var currentPage, level;

    currentPage = 0;
    level = 1;
    showPage(currentPage);
    return $('html').keydown(function(e) {
      switch (e.which) {
        case 38:
          e.preventDefault();
          if (level > 0) {
            level--;
          }
          $('h1').animate({
            fontSize: '70'
          }, 1000, function() {});
          return $(PAGE).show();
        case 40:
          e.preventDefault();
          level++;
          $("" + PAGE + ":eq(" + currentPage + ")").scrollTop();
          return $('h1').animate({
            fontSize: '200'
          }, 1000, function() {
            return showPage(currentPage);
          });
        case 37:
          e.preventDefault();
          if (currentPage > 0) {
            currentPage--;
          }
          return showPage(currentPage);
        case 39:
          e.preventDefault();
          if (currentPage < $(PAGE).size() - 1) {
            currentPage++;
          }
          return showPage(currentPage);
      }
    });
  });

  showPage = function(index) {
    $(PAGE).hide();
    return $("" + PAGE + ":eq(" + index + ")").show();
  };

}).call(this);
