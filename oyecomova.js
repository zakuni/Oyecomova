// Generated by CoffeeScript 1.6.2
(function() {
  var PAGES, PJAX, REPEAT, showNextPage, showPage, showPreviousPage, zoomIn, zoomOut;

  PAGES = 'section';

  PJAX = false;

  REPEAT = true;

  $(function() {
    var currentPage, level;

    currentPage = 0;
    level = 1;
    $(PAGES).css('transition', 'all 1s ease');
    showPage(currentPage);
    return $('html').keydown(function(e) {
      switch (e.which) {
        case 38:
          e.preventDefault();
          if (level > 0) {
            level--;
          }
          zoomOut();
          return $(PAGES).show();
        case 40:
          e.preventDefault();
          level++;
          $("" + PAGES + ":eq(" + currentPage + ")").scrollTop();
          zoomIn();
          return showPage(currentPage);
        case 37:
          e.preventDefault();
          $('h1').removeClass('zoomin').removeClass('zoomout');
          return currentPage = showPreviousPage(currentPage);
        case 39:
          e.preventDefault();
          $('h1').removeClass('zoomin').removeClass('zoomout');
          return currentPage = showNextPage(currentPage);
      }
    });
  });

  zoomIn = function() {
    $('h1').removeClass('zoomout');
    return $('h1').addClass('zoomin');
  };

  zoomOut = function() {
    $('h1').removeClass('zoomin');
    return $('h1').addClass('zoomout');
  };

  showPreviousPage = function(page) {
    if (page > 0) {
      page--;
    } else {
      if (REPEAT) {
        page = $(PAGES).size() - 1;
      }
    }
    showPage(page);
    return page;
  };

  showNextPage = function(page) {
    if (page < $(PAGES).size() - 1) {
      page++;
    } else {
      if (REPEAT) {
        page = 0;
      }
    }
    showPage(page);
    return page;
  };

  showPage = function(index) {
    return $(PAGES).css('transform', "translateX(-" + (index * 100) + "%)");
  };

}).call(this);
